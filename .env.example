# Anthropic API key — used by src/pipeline/extract-events.ts and pre-filter.ts.
# In iteration 1 the pipeline does not exist yet and this is unused; it is
# included so the eventual GitHub Actions workflow has a documented secret name.
ANTHROPIC_API_KEY=
